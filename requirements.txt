# Runtime dependencies only. Kept in sync with [project.dependencies] in pyproject.toml by
# tests/test_packaging.py -- the two disagreed in the baseline (defect PKG-003).
#
# Development and test dependencies live in requirements-dev.txt.
psutil>=5.9.0
typer>=0.9.0
rich>=13.0.0
loguru>=0.7.0
